@using System.Collections.Generic;
@using Todo.WebApp.Controllers
@using Todo.WebApp.SharedModels;
@using static Todo.WebApp.Controllers.ControllerExtensions;
@model List<TodoListSummary>;

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Your lists</h1>

<ul class="list-group">
@foreach (var summary in Model)
{
    <li class="list-group-item">
        <a href="
            @Url.Action(
                nameof(TodoListController.Get),
                RoutingName<TodoListController>(),
                new { listId = summary.Id }
            )
        ">
            @summary.Title
        </a>
    </li>
}
</ul>
